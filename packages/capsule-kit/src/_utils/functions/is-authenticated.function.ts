//import { userStore } from "../../stores/users/user.store";

export const isAuthenticated = (): boolean => {
  //const { access_token, userId } = userStore.getValues();
  return true; //!!access_token && !!userId;
};
