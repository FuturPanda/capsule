{
  "name": "flint",
  "version": "0.0.0",
  "description": "SDK for capsule backend",
  "main": "index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "jest",
    "lint": "eslint 'src/**/*.{js,ts}'",
    "format": "prettier --write 'src/**/*.{js,ts}'",
    "prepublishOnly": "npm run build"
  },
  "author": "futur_panda",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.8.6",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  }
}
